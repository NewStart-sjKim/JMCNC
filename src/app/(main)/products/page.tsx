// Server Component: Firebase Admin SDK로 products 컬렉션 조회 (SEO를 위해 클라이언트 fetch 금지 — PROJECT.md 아키텍처 원칙 1)
export default async function ProductsPage() {
  // TODO: const products = await getProducts();
  return <div>Products</div>;
}
